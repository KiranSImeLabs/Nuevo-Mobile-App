import 'package:flutter/material.dart';

class DailyExerciseScreen extends StatelessWidget {
  const DailyExerciseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Daily Exercise'),
      ),
      body: const Center(
        child: Text('Daily Exercise Content Coming Soon'),
      ),
    );
  }
}
